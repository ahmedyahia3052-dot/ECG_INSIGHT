import appColors from "@/constants/colors";

/** Application-level tokens (radius, gradients, palette keys). */
export { default as appColors } from "@/constants/colors";
export { medicalTheme, type MedicalTheme } from "@/theme/medicalTheme";

export const appRadius = appColors.radius;
export const appGradients = appColors.gradients;
